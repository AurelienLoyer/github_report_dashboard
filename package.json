{
  "name": "github_report",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js | cd front && npm install && npm run build"
  },
  "author": "Aurélien Loyer",
  "license": "ISC",
  "dependencies": {
    "bluebird": "^3.5.0",
    "express": "^4.15.2",
    "github": "^9.2.0",
    "oauth": "^0.9.15",
    "socket.io": "^1.7.3"
  },
  "engines": {
    "node": "6.9.2"
  }
}
